# Example environment file for Docker build configuration
# Copy this to build.env and customize as needed
# Usage: source build.env && ./build.sh

# Stable-Baselines3 Configuration
# Use 'master' for latest, or specify a commit hash/tag for reproducibility
SB3_REPO=https://github.com/roughscale/stable-baselines3.git
SB3_BRANCH=master

# Stable-Baselines3-Contrib Configuration
SB3_CONTRIB_REPO=https://github.com/roughscale/stable-baselines3-contrib.git
SB3_CONTRIB_BRANCH=master

# AWS Configuration (script pushes to ECR by default)
# AWS_ACCOUNT_ID is auto-detected from your AWS profile if not set
# Uncomment and set explicitly only if you want to override auto-detection
# AWS_ACCOUNT_ID=123456789012

AWS_REGION=ap-southeast-2
AWS_PROFILE=default  # Or specify a custom profile name

# Note: To build without pushing, use: ./build.sh --build-only

# Environment and Tagging
ENVIRONMENT=research
IMAGE_TAG=latest

# Examples of using specific commits for reproducibility:
# SB3_BRANCH=abc123def456789  # Full commit hash
# SB3_CONTRIB_BRANCH=v1.2.3   # Tag
# IMAGE_TAG=2024-12-27        # Date-based tag
